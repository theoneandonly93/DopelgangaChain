{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "if [ -f frontend/package.json ]; then (cd frontend && npm install --no-audit --no-fund --legacy-peer-deps); else npm install --no-audit --no-fund --legacy-peer-deps; fi",
  "buildCommand": "if [ -f frontend/package.json ]; then (cd frontend && npm run build); else npm run build; fi",
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}
