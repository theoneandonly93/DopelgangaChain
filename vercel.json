{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "npm --prefix frontend install --include=dev --no-audit --no-fund",
  "buildCommand": "npm --prefix frontend run build",
  "outputDirectory": "frontend/.next",
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}
